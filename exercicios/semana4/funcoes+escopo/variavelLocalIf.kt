fun main () {
    val idade = 20
    if (idade >= 18) {
        val mensagem = "Maior de Idade"
        println(mensagem)
    }
    //println(mensagem) // <== realmente nao funciona tentando imprimir fora do escopo.
}